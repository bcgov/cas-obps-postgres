apiVersion: v1
kind: Service
metadata:
  name: {{ include "cas-obps-postgres.fullname" . }}
  labels:
    {{- include "cas-obps-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cas-obps-postgres.selectorLabels" . | nindent 4 }}
